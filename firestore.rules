rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC: Anyone can read & write (use only if you're comfortable with this)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}