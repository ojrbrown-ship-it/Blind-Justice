rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ PUBLIC: Anyone on the internet can read & write everything
    match /{document=**} {
      allow read, write: if true;
    }
  }
}